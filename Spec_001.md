In the HelloGrain we are not getting the logging values we need.  Our records need the date appended to the log entry in the SayHello(...) method.  Add a log entry
that outputs "This call happened on yyyymmdd" to that method.
